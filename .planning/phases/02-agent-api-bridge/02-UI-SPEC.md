---
phase: 2
slug: agent-api-bridge
status: approved
reviewed_at: 2026-03-16
shadcn_initialized: false
preset: none
created: 2026-03-16
---

# Phase 2 — UI Design Contract

> Visual and interaction contract for the Agent API Bridge phase. Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — Flutter Material 3 with custom BrandTheme |
| Preset | not applicable |
| Component library | Flutter Material 3 (Material.dart) |
| Icon library | Material Icons (Icons.*) |
| Font | System default (no custom font declared in pubspec) |

Source: `lib/theme/brand_theme.dart` — buildBrandTheme() with useMaterial3: true, brightness: Brightness.dark.

No shadcn gate applies. This is a Flutter/Dart project with a fully declared BrandTheme. All design tokens are sourced directly from `lib/theme/brand_theme.dart`.

---

## Spacing Scale

Declared values (must be multiples of 4):

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding (e.g. SizedBox(height: 4)) |
| sm | 8px | Tight element spacing (e.g. between label and field, SizedBox(height: 8)) |
| md | 16px | Default element spacing within sections (SizedBox(height: 16)) |
| lg | 24px | Section padding, page-level horizontal padding (EdgeInsets.all(24)) |
| xl | 32px | Major section breaks (SizedBox(height: 32)) |
| 2xl | 48px | Logomark bottom padding, large vertical gaps |
| 3xl | 64px | Not yet used; reserved for page-level spacing if needed |

Exceptions:
- Key row list items: use 12px vertical gap (SizedBox(height: 12)) between section header and first row — matches `SecuritySettingsSection` pattern (source: `security_settings_section.dart` lines 57, 95)
- NavigationRail leading top gap: 8px (source: `app_shell.dart` line 130)
- Touch targets for icon buttons: minimum 44x44px per Material 3 spec

Source: All values extracted from existing `settings_screen.dart`, `security_settings_section.dart`, and `app_shell.dart` — codebase confirms 8-point scale with 12px used at section-internal sub-spacing.

---

## Typography

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Display | 24px | 700 (bold) | 1.2 | Page title ("Agent API") — matches `settings_screen.dart` line 74 |
| Heading | 16px | 700 (bold) | 1.2 | Dialog titles, card section titles — matches `_ChangePasswordDialog` title line 192 |
| Body | 14px | 400 (regular) | 1.5 | List tile labels, settings rows, key labels — matches `security_settings_section.dart` lines 63, 72 |
| Caption | 12px | 400 (regular) | 1.4 | Subtitles, status descriptions, RPC URL line — matches `settings_screen.dart` line 113 |

Additional:
- Section group headers (e.g. "Security", "API Keys"): 14px, weight 700 (w700), color BrandColors.textSecondary — matches `security_settings_section.dart` line 51–55 and `settings_screen.dart` line 79–82
- Monospace code text (masked key display, curl commands): 12px, fontFamily: 'monospace', color BrandColors.textSecondary — matches `settings_screen.dart` line 185–189
- Navigation rail labels: 12px, weight 400 — matches `brand_theme.dart` navigationRailTheme lines 51–52

Source: Extracted from existing screens. Two declared weights: 400 (regular) and 700 (bold). Four size levels: 12, 14, 16, 24.

---

## Color

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | #000000 (BrandColors.background — Onyx Black) | Page background, scaffold background |
| Secondary (30%) | #141414 (BrandColors.surface — Graphite) + #1E1E1E (BrandColors.card — Charcoal) | NavigationRail background, card backgrounds, input fill, dialog backgrounds |
| Accent (10%) | #F87040 (BrandColors.primary — Solar Flare orange) | Server toggle ON state, "Create Your First Key" primary CTA button, NavigationRail selected icon + indicator, active input border focus ring |
| Destructive | #E74C3C (BrandColors.error — Ember Red) | Revoke key confirmation text, inline server error status text, inline validation errors |

Additional semantic colors (not accent, not destructive):
- Server running indicator: #2ECC71 (BrandColors.success — Crypto Green)
- Server stopped indicator: #606060 (BrandColors.textDisabled — Smoke)
- Warning/bypass actions: #E2A93B (BrandColors.warning — Tungsten) — used on destructive-adjacent buttons (see `settings_screen.dart` line 269–271)
- Border: #2A2A2A (BrandColors.border — Ash) — all card outlines, dividers, OutlinedButton default border
- Text primary: #FFFFFF — headings, list tile primary text
- Text secondary: #B0B0B0 (BrandColors.textSecondary — Fog) — section group headers, captions, masked key display

Accent reserved for: server toggle ON state, primary CTA buttons (ElevatedButton), selected nav rail item, focused input border. Never use on secondary copy, dividers, or status labels.

Source: `lib/theme/brand_theme.dart` — all BrandColors values.

---

## Component Inventory

These are the Material 3 widgets this phase uses. Executor must not reinvent; use the existing BrandTheme-styled variants.

| Component | Widget | Notes |
|-----------|--------|-------|
| Page container | `ListView(padding: EdgeInsets.all(24))` | Matches settings_screen.dart pattern — single scrollable page |
| Page title | `Text('Agent API', style: TextStyle(fontSize: 24, fontWeight: FontWeight.bold))` | First child of ListView |
| Section group header | `Text('...', style: TextStyle(fontSize: 14, fontWeight: FontWeight.w700, color: BrandColors.textSecondary))` | Matches Security/Developer Settings headers |
| Section divider | `Divider()` + `SizedBox(height: 16)` before header | Matches settings_screen.dart lines 123–133 |
| Server toggle row | `SwitchListTile` with `activeThumbColor: BrandColors.primary` | Matches Jito MEV Protection toggle pattern |
| Status indicator | `Row` with `Container(width: 10, height: 10, decoration: BoxDecoration(shape: BoxShape.circle, color: statusColor))` + `Text` label | Inline, no popup — green/red/gray circle + label |
| API key card/row | `ListTile(contentPadding: EdgeInsets.zero)` with title, subtitle (masked key), and trailing action icons | Matches change password ListTile pattern |
| Key mask display | `Text('db_••••x7f2', style: TextStyle(fontSize: 12, fontFamily: 'monospace'))` | BrandColors.textSecondary |
| Primary CTA button | `ElevatedButton` (BrandTheme: orange background, black text, 8px radius) | "Create Your First Key", "Create Key" |
| Secondary/destructive button | `OutlinedButton` (BrandTheme: border + white text) | "Revoke Key" action before auth challenge |
| Auth challenge | `showAuthChallengeDialog` — established in Phase 1 (`lib/features/lock/auth_challenge_dialog.dart`) | Required before create, delete, reveal |
| "Key created" dialog | `AlertDialog` with copyable TextField (readOnly, monospace) and "Copy & Close" ElevatedButton | Full key shown once; never stored in state after dialog closes |
| Endpoint dropdown | `DropdownButton<String>` (underline: SizedBox.shrink()) | Matches existing DropdownButton usage in settings |
| Curl command display | `SelectableText` in `Container` with `BrandColors.card` background, 8px border radius, monospace 12px | Allows user to select/copy without a separate copy button (or with one) |
| Copy button | `Tooltip(message: 'Copy to clipboard', child: IconButton(icon: Icon(Icons.copy, size: 20)))` | Adjacent to curl display and key row — Tooltip required; icon-only button must not be bare |
| Reveal button | `Tooltip(message: 'Reveal key', child: IconButton(icon: Icon(Icons.visibility_outlined, size: 20)))` | On key row — Tooltip required; icon-only button must not be bare |
| Empty state | `Column` centered with `Icon(Icons.lan_outlined, size: 48, color: BrandColors.textDisabled)` + explanation text + ElevatedButton CTA | See Copywriting Contract |
| Swipe-to-delete | `Dismissible` with `direction: DismissDirection.endToStart`, red background (`BrandColors.error`), trash icon | Key row swipe reveals revoke; requires confirm dialog then auth challenge |
| Loading spinner | `SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2))` | Matches `_ChangePasswordDialog` loading state line 287–290 |

---

## Interaction Contracts

### Server Toggle
- Toggle is a `SwitchListTile`. Disabled (grayed, `onChanged: null`) when no API keys exist.
- ON: calls `startAgentServer()` FRB async. While starting, toggle thumb shows `CircularProgressIndicator` (20px, strokeWidth: 2). On success: green status indicator + "Running on :9876". On failure: red indicator + error message inline below toggle row — no popup or toast.
- OFF: calls `stopAgentServer()` FRB sync. Immediately shows gray indicator + "Stopped".
- Status persisted to SharedPreferences `agent_server_enabled`. Auto-starts on launch if flag is true AND active wallet is available.

### API Key Creation Flow
1. User taps "Create Key" (or "Create Your First Key" in empty state).
2. `showAuthChallengeDialog` — requires app password. On failure: returns to page, no key created.
3. On auth success: show `AlertDialog` with optional label `TextField` (hint: "e.g. Claude agent") + "Create" `ElevatedButton`.
4. On create: FRB `createApiKey(label)` returns full token string. Dialog updates in-place to show full token in read-only monospace `TextField` with "Copy & Close" button. Token is NOT stored in Riverpod state — only returned once.
5. Dialog closes: key list refreshes showing new row with masked key (`db_••••x7f2`).

### API Key Reveal Flow
1. User taps `Tooltip(message: 'Reveal key', child: IconButton(...))` on a key row.
2. `showAuthChallengeDialog` — requires app password.
3. On auth success: show `AlertDialog` with full token in read-only monospace `TextField` + "Copy & Close" button.
4. Dialog close: row returns to masked state.

### API Key Revocation Flow
1. User swipes key row end-to-start. `Dismissible` reveals red background with `Icons.delete`.
2. On dismiss threshold reached: show `AlertDialog` — "Revoke API key?" with label, "Keep Key" `TextButton` + "Revoke Key" `OutlinedButton` (foreground: BrandColors.error).
3. On confirm: `showAuthChallengeDialog` — requires app password.
4. On auth success: FRB `revokeApiKey(token)` — updates both config on disk AND live `AppState.api_tokens` in running server.
5. Key row removed from list immediately. If last key was revoked: server toggle is disabled and server is stopped.

### Quick Test Section
- `DropdownButton` lists all 6 endpoints: `/health`, `/wallet`, `/balance`, `/tokens`, `/price`, `/history`.
- Auto-selects first key if exactly one key exists; shows key selector dropdown if multiple keys exist.
- Selecting endpoint or key regenerates curl command immediately (no submit button needed).
- Curl display is `SelectableText` in a code block styled card. Adjacent `Tooltip(message: 'Copy to clipboard', child: IconButton(Icons.copy))` copies to clipboard.
- Clipboard auto-clears after 30 seconds (Phase 6 polish requirement PLSH-02 — implement the timer here proactively since this is the canonical sensitive-copy use case).

### NavigationRail (app_shell.dart)
- 5th destination added between Contacts (index 2) and Settings (index 4).
- Icon: `Icons.lan_outlined` (unselected), `Icons.lan` (selected).
- Label: `Text('Agent API')`.
- Route: `/agent-api` added to `_routes` list at index 3; Settings shifts to index 4.

---

## Screen Layout: AgentApiScreen

Single scrollable `ListView(padding: EdgeInsets.all(24))`:

```
[Page Title] "Agent API"                           24px bold
[SizedBox 24px]

[Section Header] "Server"                         14px w700 textSecondary
[SizedBox 12px]
[SwitchListTile]  "Agent Server"
  subtitle: "Allow AI agents to connect on localhost:9876"
[SizedBox 8px]
[Status Row]  ● Running on :9876  |  ● Stopped  |  ● Error: <message>
[SizedBox 32px]

[Divider]
[SizedBox 16px]
[Section Header] "API Keys"                       14px w700 textSecondary
[SizedBox 12px]
[Key Row × N]  label / masked-key / reveal-icon / copy-icon
  [Dismissible → swipe to revoke]
[SizedBox 8px]
[OutlinedButton] "+ Create Key"  (full width if 0 keys → replaced by empty state)
[SizedBox 32px]

[Divider]
[SizedBox 16px]
[Section Header] "Quick Test"                     14px w700 textSecondary
[SizedBox 12px]
[Row]  [DropdownButton endpoint]  [DropdownButton key (if >1 key)]
[SizedBox 8px]
[Code block card]  curl -H "Authorization: Bearer <token>" http://localhost:9876<endpoint>
[Row]  [Spacer]  [IconButton copy]
```

Primary focal point: the server toggle ON/OFF state — the orange accent (`BrandColors.primary`) thumb and active indicator is the first element the eye should resolve on the populated screen; everything below it (keys, curl test) is secondary.

**Empty State (no keys yet):**
Replaces the keys list + create button with a centered `Column`:
```
[SizedBox 48px]
[Icon(Icons.lan_outlined, size: 48, color: BrandColors.textDisabled)]
[SizedBox 16px]
[Text heading] "Connect AI Agents"                16px bold, textPrimary, centered
[SizedBox 8px]
[Text body] "AI agents can query your balances,   14px, textSecondary, centered, maxWidth 320px
  request transactions, and more. Create
  an API key to get started."
[SizedBox 24px]
[ElevatedButton] "Create Your First Key"          full width (max 320px)
[SizedBox 48px]
```
Server toggle is visible above empty state but disabled (`onChanged: null`, opacity 0.5).
Quick Test section is hidden when 0 keys exist.

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Page title | Agent API |
| Server section header | Server |
| Server toggle label | Agent Server |
| Server toggle subtitle | Allow AI agents to connect on localhost:9876 |
| Status: running | Running on :9876 |
| Status: stopped | Stopped |
| Status: error | Error: {error message from Rust} |
| API keys section header | API Keys |
| Key creation button (has keys) | + Create Key |
| Key creation button (empty state) | Create Your First Key |
| Empty state heading | Connect AI Agents |
| Empty state body | AI agents can query your balances, request transactions, and more. Create an API key to get started. |
| Quick test section header | Quick Test |
| Create key dialog title | Create API Key |
| Create key label placeholder | e.g. Claude agent, Trading bot |
| Create key label default (blank) | API Key 1, API Key 2, … |
| Key created dialog title | API Key Created |
| Key created dialog body | Copy this key now. It will not be shown again. |
| Key created copy button | Copy & Close |
| Reveal key dialog title | API Key |
| Reveal key dialog body | This is your full API key. Keep it secret. |
| Reveal key copy button | Copy & Close |
| Revoke confirmation dialog title | Revoke API key? |
| Revoke confirmation dialog body | "{label}" will be permanently revoked. Any agent using it will lose access immediately. |
| Revoke confirmation cancel | Keep Key |
| Revoke confirmation action | Revoke Key |
| Error: port in use | Port 9876 is in use by another process. Close it and try again. |
| Error: no keys on start attempt | Create at least one API key before starting the server. |
| Error: max keys reached | Maximum of 10 API keys allowed. Revoke an existing key first. |
| Soft-limit warning (10 keys) | 10 / 10 keys — revoke one to create another. |

---

## Destructive Actions

| Action | Trigger | Confirmation | Auth Required |
|--------|---------|--------------|---------------|
| Revoke API key | Swipe-to-delete on key row | AlertDialog with label, "Keep Key" + "Revoke Key" buttons | Yes — `showAuthChallengeDialog` after confirm dialog |
| Server stop (implicit, last key revoked) | Triggered automatically when 0 keys remain | No separate confirmation — described in revoke copy | Covered by revoke auth |

No other destructive actions in this phase. Intent deletion is Phase 3 scope.

---

## Accessibility

- All interactive elements: minimum 44x44px touch target (Material 3 default; confirm via `IconButton` minTapSize)
- Status indicator circles: must include `Semantics(label: 'Server status: running')` wrapper — color alone does not convey state
- Key masked display: `Semantics(label: 'API key, masked. Tap eye icon to reveal.')` on the masked text widget
- Copy icon button: wrapped as `Tooltip(message: 'Copy to clipboard', child: IconButton(...))` — tooltip text serves as accessible label for screen readers
- Reveal icon button: wrapped as `Tooltip(message: 'Reveal key', child: IconButton(...))` — tooltip text serves as accessible label for screen readers
- `Dismissible` swipe: also provide a reveal-via-tap alternative (three-dot menu or explicit Revoke button in key row) for keyboard/mouse users on macOS desktop
- Error messages: rendered as `Text` inline with `BrandColors.error` — not behind a tooltip or aria-hidden

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none — Flutter project, not applicable | not applicable |
| Third-party | none declared | not applicable |

No shadcn registry. No third-party component blocks. All components are Flutter Material 3 widgets styled by the existing BrandTheme. Registry vetting gate: not applicable.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
