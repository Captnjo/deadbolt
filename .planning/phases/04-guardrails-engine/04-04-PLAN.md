---
phase: 04-guardrails-engine
plan: 04
type: execute
wave: 3
depends_on: [04-02, 04-03]
files_modified: []
autonomous: false
requirements: [GRDL-01, GRDL-02, GRDL-03, GRDL-04, GRDL-05, GRDL-06, GRDL-07, INFR-04]

must_haves:
  truths:
    - "All guardrails requirements verified by human inspection of running app"
    - "Guardrail settings UI is functional and persists across restart"
    - "Manual transactions are properly gated by guardrails"
    - "Agent transactions receive specific HTTP 403 errors when blocked"
    - "Password bypass works for manual transactions only"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the complete guardrails engine: settings UI, manual transaction enforcement, agent error responses, password bypass, and config persistence.

Purpose: Validates the end-to-end guardrails system before marking the phase complete. This is the only checkpoint in the phase — all other plans are fully autonomous.

Output: Verification pass/fail with issues list if any.
</objective>

<execution_context>
@/Users/jo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-guardrails-engine/04-CONTEXT.md
@.planning/phases/04-guardrails-engine/04-01-SUMMARY.md
@.planning/phases/04-guardrails-engine/04-02-SUMMARY.md
@.planning/phases/04-guardrails-engine/04-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end guardrails verification</name>
  <files></files>
  <action>
This is a human verification checkpoint. The executor presents the verification steps below to the user and waits for approval. No code changes are made in this task.

The verification covers all 8 requirement IDs: GRDL-01, GRDL-02, GRDL-03, GRDL-04, GRDL-05, GRDL-06, GRDL-07, INFR-04.

**Prerequisite:** FRB codegen must run before testing. The guardrails bridge stubs throw UnimplementedError until codegen generates the real bindings.

**Settings UI (GRDL-07):**
1. Open Settings screen
2. Verify "Guardrails" section appears below "Security" section
3. Verify master toggle defaults to ON (no subtitle shown)
4. Toggle OFF — verify password challenge dialog appears
5. Enter correct password — verify toggle switches to OFF and subtitle "All guardrails disabled" appears in red
6. Toggle back ON — verify no password required, subtitle disappears
7. Verify expandable card "Token Whitelist" appears when toggle is ON

**Token Whitelist (GRDL-01, GRDL-03):**
8. Expand "Token Whitelist" card — verify "No restrictions — all SPL tokens allowed" text when empty
9. Tap "Add Token" — verify bottom sheet appears with "Add Token to Whitelist" heading
10. Verify search field shows held tokens from wallet
11. Search for a token by name — verify list filters
12. Tap a token — verify it's added to whitelist and sheet dismisses
13. Verify token row shows icon, symbol, and truncated mint address
14. Tap "Add Token" again, paste a valid 44-char mint address — verify it's added
15. Paste an invalid address (wrong length) — verify error message
16. Tap X on a token row — verify it's removed
17. Verify collapsed card shows correct count (e.g., "2 tokens")

**Config Persistence (INFR-04):**
18. Add 2 tokens, toggle ON, quit and restart app
19. Verify settings persisted after restart
20. Check ~/.deadbolt/config.json for guardrails fields

**Program Whitelist (GRDL-02):**
21. Verify no program whitelist UI exists (v2 deferred)
22. Verify programWhitelist field exists in config.json

**Manual Enforcement (GRDL-04, GRDL-06):**
23-31. Test send with non-whitelisted token: banner, disabled confirm, password override
32. Test SOL send passes without whitelist check

**Swap Enforcement (GRDL-04):**
33-36. Test swap with non-whitelisted output token

**Agent Errors (GRDL-05):**
37-40. curl test for HTTP 403 with specific error message

**Master Toggle OFF:**
41-43. Verify disabling guardrails skips all checks

**One-Shot Bypass:**
44-45. Verify bypass resets after transaction
  </action>
  <verify>User types "approved" or describes issues</verify>
  <done>All 45 verification steps pass. Phase 4 guardrails engine is complete.</done>
  <read_first></read_first>
  <acceptance_criteria>
    - User confirms all verification steps pass by typing "approved"
    - Or user provides specific issues list for gap closure
  </acceptance_criteria>
</task>

</tasks>

<verification>
All manual verification steps pass — guardrails engine is complete and functional.
</verification>

<success_criteria>
All 45 verification steps pass without issues.
</success_criteria>

<output>
After completion, create `.planning/phases/04-guardrails-engine/04-04-SUMMARY.md`
</output>
