---
phase: 01-auth-system
plan: 05
type: execute
wave: 3
depends_on: ["01-03", "01-04"]
files_modified:
  - lib/features/lock/auth_challenge_dialog.dart
  - lib/features/settings/security_settings_section.dart
  - lib/features/settings/settings_screen.dart
autonomous: false
requirements: [AUTH-04, AUTH-05, AUTH-06, AUTH-07, AUTH-03]

must_haves:
  truths:
    - "Auth challenge dialog appears when user taps 'Reveal Seed Phrase' and requires correct password"
    - "Auth challenge dialog has Go Back and Verify buttons; wrong password shows inline error"
    - "Settings screen has Security section with Change Password, Auto-Lock Timeout, Lock Now"
    - "Change Password requires current password before accepting new password"
    - "Auto-Lock Timeout dropdown has options: 5 min, 15 min, 30 min, 1 hour, Never"
    - "Lock Now button immediately locks the app"
    - "Timeout setting persists across app restart via SharedPreferences"
  artifacts:
    - path: "lib/features/lock/auth_challenge_dialog.dart"
      provides: "Reusable auth challenge modal for sensitive operations"
      min_lines: 60
    - path: "lib/features/settings/security_settings_section.dart"
      provides: "Settings Security section with password change and timeout"
      min_lines: 60
  key_links:
    - from: "lib/features/lock/auth_challenge_dialog.dart"
      to: "lib/src/rust/api/auth.dart"
      via: "verifyAppPassword FFI call"
      pattern: "verifyAppPassword"
    - from: "lib/features/settings/security_settings_section.dart"
      to: "lib/providers/auth_provider.dart"
      via: "lock() and setIdleTimeout()"
      pattern: "authProvider"
---

<objective>
Build the auth challenge dialog for sensitive operations and the Settings security section.

Purpose: AUTH-04/05/06 require password re-entry for sensitive operations (reveal mnemonic, API key management, guardrail bypass). AUTH-07 requires password change in settings. AUTH-03 requires configurable idle timeout. This plan adds both the reusable auth challenge modal and the full Settings security section.

Output: AuthChallengeDialog widget (reusable across phases), SecuritySettingsSection widget (embedded in existing SettingsScreen), checkpoint for human verification of the full auth flow.
</objective>

<execution_context>
@/Users/jo/.claude/get-shit-done/workflows/execute-plan.md
@/Users/jo/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-auth-system/01-CONTEXT.md
@.planning/phases/01-auth-system/01-RESEARCH.md
@.planning/phases/01-auth-system/01-UI-SPEC.md
@.planning/phases/01-auth-system/01-03-SUMMARY.md
@.planning/phases/01-auth-system/01-04-SUMMARY.md

<interfaces>
<!-- From Plan 01 — FFI bridge auth functions -->
From lib/src/rust/api/auth.dart:
```dart
Future<void> verifyAppPassword({required String password});
Future<void> changeAppPassword({required String current, required String newPassword});
```

From lib/providers/auth_provider.dart (Plan 03):
```dart
final authProvider = StateNotifierProvider<AuthNotifier, AuthState>(...);
final idleTimeoutSecondsProvider = StateProvider<int>(...);
Future<void> setIdleTimeout(WidgetRef ref, int seconds);
// AuthNotifier has: lock(), unlock(), resetActivity()
```

From lib/features/settings/settings_screen.dart:
```dart
class SettingsScreen extends ConsumerStatefulWidget { ... }
// Has: Preferences section, Developer Settings section
// Pattern: ListView with EdgeInsets.all(24), section headers with
// TextStyle(fontSize: 14, fontWeight: FontWeight.w600, color: BrandColors.textSecondary)
// Uses Divider() + SizedBox(height: 16) between sections
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AuthChallengeDialog and SecuritySettingsSection</name>
  <files>
    lib/features/lock/auth_challenge_dialog.dart,
    lib/features/settings/security_settings_section.dart,
    lib/features/settings/settings_screen.dart
  </files>
  <read_first>
    lib/features/settings/settings_screen.dart,
    lib/providers/auth_provider.dart,
    lib/theme/brand_theme.dart,
    lib/src/rust/api/auth.dart
  </read_first>
  <action>
    **1. Create `lib/features/lock/auth_challenge_dialog.dart`:**

    Following the UI-SPEC layout for "Auth Challenge Dialog":

    Top-level helper function:
    ```dart
    /// Shows auth challenge dialog. Returns true if password verified, false if cancelled.
    Future<bool> showAuthChallengeDialog(BuildContext context) async {
      final result = await showDialog<bool>(
        context: context,
        barrierDismissible: false,
        builder: (_) => const AuthChallengeDialog(),
      );
      return result ?? false;
    }
    ```

    `class AuthChallengeDialog extends ConsumerStatefulWidget`

    State:
    - `TextEditingController _passwordController`
    - `bool _obscure = true`
    - `bool _loading = false`
    - `String? _error`

    Build: `AlertDialog` with:
    - `title: Text('Confirm Password', style: TextStyle(fontSize: 16, fontWeight: FontWeight.bold))`
    - `content: Column(mainAxisSize: MainAxisSize.min, ...)` containing:
      - Text "Enter your app password to continue." style: `TextStyle(fontSize: 14, color: BrandColors.textSecondary)`
      - SizedBox(height: 16)
      - Password TextField with visibility toggle:
        - `obscureText: _obscure`
        - suffixIcon with eye icon, `Semantics(label: _obscure ? 'Show password' : 'Hide password')`
        - `onSubmitted: (_) => _verify()` (Enter key submits)
      - If `_error != null`: Padding + Text `_error` in `TextStyle(fontSize: 14, color: BrandColors.error)`
    - `actions`:
      - `TextButton(onPressed: () => Navigator.pop(context, false), child: Text('Go Back'))`
      - `ElevatedButton(onPressed: _loading ? null : _verify, child: _loading ? SizedBox(width: 20, height: 20, child: CircularProgressIndicator(strokeWidth: 2)) : Text('Verify'))`

    `_verify()` method:
    1. Set `_loading = true`, `_error = null`, setState.
    2. `try { await auth_bridge.verifyAppPassword(password: _passwordController.text); Navigator.pop(context, true); }`
    3. `catch (e) { setState(() { _error = 'Incorrect password. Please try again.'; _loading = false; }); _passwordController.clear(); }`

    **2. Create `lib/features/settings/security_settings_section.dart`:**

    Following the UI-SPEC layout for "Security Settings Section":

    `class SecuritySettingsSection extends ConsumerStatefulWidget`

    Build method returns a `Column` with:
    - `Divider()`
    - SizedBox(height: 16)
    - Section header: `Text('Security', style: TextStyle(fontSize: 14, fontWeight: FontWeight.w700, color: BrandColors.textSecondary))`
    - SizedBox(height: 12)
    - `ListTile` "Change Password" with trailing `Icon(Icons.chevron_right)`:
      - `onTap: _showChangePasswordDialog`
    - `ListTile` "Auto-Lock Timeout" with trailing `DropdownButton<int>`:
      - Items: `{300: '5 minutes', 900: '15 minutes', 1800: '30 minutes', 3600: '1 hour', 0: 'Never'}`
      - Value: `ref.watch(idleTimeoutSecondsProvider)`
      - `onChanged: (value) { if (value != null) setIdleTimeout(ref, value); }`
      - `underline: SizedBox.shrink()` (consistent with existing Display Currency dropdown)
    - SizedBox(height: 12)
    - Full-width `OutlinedButton.icon`:
      - `icon: Icon(Icons.lock_outline, size: 18)`
      - `label: Text('Lock Now')`
      - `onPressed: () { ref.read(authProvider.notifier).lock(); }`
      - No confirmation needed (lock is reversible)

    `_showChangePasswordDialog` method:
    Shows a dialog with:
    - Current password field
    - New password field
    - Confirm new password field
    - PasswordStrengthMeter on new password
    - Cancel and Save buttons
    - Validates: current password correct (via FFI), new password >= 8 chars, new == confirm
    - On success: calls `auth_bridge.changeAppPassword(current: current, newPassword: newPw)`
    - Shows SnackBar "Password updated" on success

    **3. Update `lib/features/settings/settings_screen.dart`:**

    Add `SecuritySettingsSection()` widget to the ListView children. Insert it AFTER the Preferences section and BEFORE the Developer Settings section:

    After the existing `SwitchListTile` for Jito MEV Protection and before the existing `const SizedBox(height: 32), const Divider(), const SizedBox(height: 16),` that precedes "Developer Settings":

    Insert: `const SecuritySettingsSection(),`

    Add import: `import 'security_settings_section.dart';`

    NOTE: The SecuritySettingsSection already includes its own Divider at the top, so the existing Divider before "Developer Settings" should remain. The visual order will be:
    - Preferences section
    - [SecuritySettingsSection with its own Divider]
    - Divider
    - Developer Settings section
  </action>
  <verify>
    <automated>cd /Users/jo/Projects/GSDtest && flutter analyze lib/features/lock/auth_challenge_dialog.dart lib/features/settings/security_settings_section.dart lib/features/settings/settings_screen.dart 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/lock/auth_challenge_dialog.dart contains `Future<bool> showAuthChallengeDialog(`
    - lib/features/lock/auth_challenge_dialog.dart contains `class AuthChallengeDialog extends ConsumerStatefulWidget`
    - lib/features/lock/auth_challenge_dialog.dart contains `Confirm Password`
    - lib/features/lock/auth_challenge_dialog.dart contains `Enter your app password to continue.`
    - lib/features/lock/auth_challenge_dialog.dart contains `Go Back`
    - lib/features/lock/auth_challenge_dialog.dart contains `Verify`
    - lib/features/lock/auth_challenge_dialog.dart contains `Incorrect password. Please try again.`
    - lib/features/lock/auth_challenge_dialog.dart contains `barrierDismissible: false`
    - lib/features/lock/auth_challenge_dialog.dart contains `verifyAppPassword`
    - lib/features/settings/security_settings_section.dart contains `class SecuritySettingsSection`
    - lib/features/settings/security_settings_section.dart contains `Change Password`
    - lib/features/settings/security_settings_section.dart contains `Auto-Lock Timeout`
    - lib/features/settings/security_settings_section.dart contains `Lock Now`
    - lib/features/settings/security_settings_section.dart contains `5 minutes` and `15 minutes` and `Never`
    - lib/features/settings/security_settings_section.dart contains `changeAppPassword`
    - lib/features/settings/security_settings_section.dart contains `Password updated`
    - lib/features/settings/settings_screen.dart contains `SecuritySettingsSection`
    - `flutter analyze` on all three files reports no errors
  </acceptance_criteria>
  <done>
    AuthChallengeDialog created as reusable modal for sensitive operations. SecuritySettingsSection created with Change Password, Auto-Lock Timeout dropdown (5/15/30/60 min + Never), and Lock Now button. Settings screen includes the new Security section between Preferences and Developer Settings.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete auth system end-to-end</name>
  <files>n/a</files>
  <action>
    Human verifies the complete Phase 1 auth system works end-to-end. All code was built in prior tasks and plans. This checkpoint confirms visual correctness, interaction flow, and functional completeness before marking the phase done.
  </action>
  <verify>
    <automated>cd /Users/jo/Projects/GSDtest && flutter analyze lib/ 2>&1 | grep -E "error|No issues" | tail -5</automated>
  </verify>
  <done>Human confirms all 9 verification steps pass. Phase 1 auth system is functionally complete.</done>
  <what-built>
    Complete Phase 1 auth system: Rust auth layer with password hash in Keychain + AtomicBool lock gate, FFI bridge, Flutter AuthProvider with idle timer, lock screen with shake animation and escalating delay, onboarding password step with strength meter, auth challenge dialog, Settings security section with password change and timeout config, INFR-06 constant-time fix, INFR-07 zeroization audit.
  </what-built>
  <how-to-verify>
    1. **Fresh launch flow:** Run the app (`flutter run -d macos`). If this is first launch after reset, you should see the onboarding wizard. The second step (after Welcome) should be "Create App Password" with a strength meter. Set a password (at least 8 chars). Verify the strength meter shows Weak/Fair/Strong as you type. Verify Continue is disabled until password >= 8 chars and passwords match. Complete onboarding.

    2. **Lock on launch:** Quit and relaunch the app. You should see the lock screen with Deadbolt logo, tagline, and password field — NOT the dashboard.

    3. **Wrong password:** Enter a wrong password and press Unlock. Verify: shake animation on the field, "Incorrect password" message. Try again — verify a delay appears ("Please wait 1s..."). Try a third time — verify 2s delay.

    4. **Correct password:** Enter the correct password. Verify: brief loading spinner, then fade transition to dashboard.

    5. **Idle timeout:** Go to Settings > Security. Verify the Security section shows: Change Password, Auto-Lock Timeout (dropdown), Lock Now. Change timeout to 5 minutes (or shorter for testing). Wait for timeout — app should show lock screen.

    6. **Lock Now:** Go to Settings > Security > Lock Now. App should immediately show lock screen.

    7. **Auth challenge:** Unlock the app. Go to any wallet's mnemonic reveal (if available). Verify the "Confirm Password" dialog appears with Go Back and Verify buttons.

    8. **Change password:** Go to Settings > Security > Change Password. Enter current password and new password. Verify "Password updated" snackbar. Lock and unlock with the new password.

    9. **Activity reset:** Unlock the app. Move mouse and type — verify the idle timer resets (app does not lock while active).
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues</resume-signal>
</task>

</tasks>

<verification>
- `flutter analyze` passes on all files in lib/features/lock/, lib/features/settings/, lib/providers/
- Auth challenge dialog verifies password via Rust FFI
- Settings security section persists timeout to SharedPreferences
- Lock Now immediately triggers lock via AuthProvider
- Full auth flow works end-to-end (manual verification in checkpoint)
</verification>

<success_criteria>
- Auth challenge dialog gates sensitive operations with password re-entry
- Settings Security section shows: Change Password, Auto-Lock Timeout (5/15/30/60 min/Never), Lock Now
- Change Password requires current password, validates new >= 8 chars, stores via FFI
- Timeout setting persists across app restart
- Lock Now immediately locks the app
- Human verification confirms end-to-end auth flow works
</success_criteria>

<output>
After completion, create `.planning/phases/01-auth-system/01-05-SUMMARY.md`
</output>
